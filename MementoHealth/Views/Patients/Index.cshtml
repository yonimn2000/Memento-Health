@model IEnumerable<MementoHealth.Entities.Patient>

@{
    ViewBag.Title = "Patients";
    ViewBag.UseFullScreenWidth = true;
}

<div class="box" style="text-align: center">
    <div class="page-title font-weight-bold h2">@ViewBag.Title</div>
    <div style="margin-top:-30px; margin-bottom: 10px">
        @Html.ActionLink("Add", "Create") |
        @Html.ActionLink("Import", "Import")
    </div>
</div>

<<<<<<< Updated upstream
<div class="container" style="overflow-y: auto; white-space: nowrap; min-width: 94.5%; text-align: center">
    @if (TempData["StatusMessage"] != null)
=======
<p>
    @Html.ActionLink("Add", "Create") |
    @Html.ActionLink("Import", "Import") |
    @Html.ActionLink("Search", "Search")
</p>
<table class="table">
    <tr>
        <th>@Html.DisplayNameFor(model => model.FullName)</th>
        <th>@Html.DisplayNameFor(model => model.Birthday)</th>
        <th>@Html.DisplayNameFor(model => model.ExternalPatientId)</th>
        <th>Actions</th>
    </tr>

    @foreach (var item in Model)
>>>>>>> Stashed changes
    {
        <p class="text-info">@TempData["StatusMessage"]</p>
    }

    <div class="card shadow">
        <div class="table-responsive">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.FullName)</th>
                        <th>@Html.DisplayNameFor(model => model.Birthday)</th>
                        <th>@Html.DisplayNameFor(model => model.ExternalPatientId)</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.FullName)</td>
                        <td>@item.Birthday.ToShortDateString()</td>
                        <td>@Html.DisplayFor(modelItem => item.ExternalPatientId)</td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.PatientId }) |
                            @Html.ActionLink("Details", "Details", new { id = item.PatientId }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.PatientId })
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>