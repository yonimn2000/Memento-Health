@model MementoHealth.Models.IndexViewModel
@{
    ViewBag.Title = "Manage";
    ViewBag.UseFullScreenWidth = true;
}

<h2>Manage Account</h2>

<p class="text-success" style="text-align:center">@ViewBag.StatusMessage</p>
<div class="container" style="text-align: center; width: 400px">
    <dl class="dl-horizontal" style="text-align: center">
        <dt class="text-left">Role:</dt>
        <dd class="text-right">@Model.Role</dd>

        <dt style="text-align: left">Password:</dt>
        <dd style="text-align: right">
            @if (Model.HasPassword)
            {
                @Html.ActionLink("Change your password", "ChangePassword")
            }
            else
            {
                @Html.ActionLink("Create", "SetPassword")
            }
        </dd>

        <dt style="text-align: left">PIN Code:</dt>
        <dd style="text-align: right">
            @Html.ActionLink("Change your PIN code", "ChangePin")
        </dd>

        <dt class="text-left">Full Name:</dt>
        <dd class="text-right">
            @Model.FullName
            <br />
            @Html.ActionLink("Change", "AddPhoneNumber")
        </dd>

        <dt style="text-align: left">Phone Number:</dt>
        <dd style="text-align: right">
            @(Model.PhoneNumber ?? "None")
            <br />
            @if (Model.PhoneNumber != null)
            {
                <text>@Html.ActionLink("Change", "AddPhoneNumber")</text>
            }
            else
            {
                <text>@Html.ActionLink("Add", "AddPhoneNumber")</text>
            }
        </dd>
    </dl>
</div>

@section styles {
    <style>
        dl {
            display: flex;
            flex-flow: row;
            flex-wrap: wrap;
            width: 100%;
            overflow: visible;
        }

        dt {
            flex: 0 0 50%;
            text-overflow: ellipsis;
            overflow: hidden;
        }

        dd {
            flex: 0 0 50%;
            margin-left: auto;
            text-align: left;
            text-overflow: ellipsis;
            overflow: hidden;
            margin-bottom: 16px;
        }

        h2 {
            display: flex;
            flex-direction: row;
            padding-bottom: 25px;
        }

            h2:before, h2:after {
                content: "";
                flex: 1 1;
                border-bottom: 1px solid;
                margin: auto;
                color: #e5e5e5
            }

            h2:before {
                margin-right: 10px
            }

            h2:after {
                margin-left: 10px
            }
    </style>
}